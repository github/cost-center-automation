# .syncignore
# Files that should NOT be overwritten during template sync
# This protects consumer-specific customizations

# Consumer's custom configuration
config/config.yaml

# Consumer's custom Renovate configuration (if they have one)
.github/renovate.json

# Consumer's custom secrets and environment-specific files
.env
.env.local
.env.production

# Consumer's custom documentation additions
CUSTOM_*.md
*_CUSTOM.md

# Consumer's private/internal workflows (if any)
.github/workflows/internal-*.yml
.github/workflows/private-*.yml

# Consumer's custom automation scripts
automation/custom_*
automation/*_custom.*

# Local development files that consumers might create
docker-compose.override.yml
docker-compose.local.yml

# Consumer's custom source modifications
src/custom_*
src/*_custom.*

# Test files and data that consumers might add
test/
tests/
__tests__/
*.test.*
test_*

# Consumer's deployment and infrastructure files
deploy/
deployment/
k8s/
terraform/
.github/workflows/deploy*.yml

# Consumer's CI/CD customizations
.github/workflows/ci*.yml
.github/workflows/cd*.yml

# IDE and editor specific files consumers might have
.vscode/settings.json
.idea/

# Consumer's custom documentation
docs/custom/
docs/internal/